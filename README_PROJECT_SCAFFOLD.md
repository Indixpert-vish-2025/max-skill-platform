Project scaffold generated per design analysis.
- Contains app group folders (auth, dashboard)
- Redux store configured at /store
- SCSS theme at /styles/theme.scss
- Bootstrap imported in app/layout.js

Next steps: implement pages and UI after approval.
